name = "BrainLyh/moontape"

version = "0.1.0"

readme = "README.md"

repository = "https://github.com/BrainLyh/moontape"

license = "Apache-2.0"

keywords = [ "har", "http", "mock", "testing", "privacy" ]

preferred_target = "native"

description = "Privacy-first HAR inspection, sanitization, and replay toolkit"

import {
  "moonbitlang/async@0.21.3",
}
